pipeline {
   agent any

libraries {
  lib('vishant-library@master')
}

    paramaters {
        params ''
    }

    stages {
        stage('check user') {
            steps{
                checkUser 'okay report'
            }
        }

	    stage('Demo') {
            steps {
                hello 'ititititi'
            }
        }
        stage('deployy') {
            steps {
                deploy 'chalo krdo abb deploy'
            }
        }
    }
}
